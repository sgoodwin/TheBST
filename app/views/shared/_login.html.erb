<h4>Sign In</h4>

<%= form_with url: "/login", method: :post do |f| %>

  <%= f.label :email %>
  <%= f.text_field :email %>
  <%= f.label :password %>
  <%= f.password_field :password %>
  <%= f.submit "Login" %>

<% end %>

<%= link_to "Sign Up", new_user_path %>
